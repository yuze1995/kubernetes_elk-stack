kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: es-master-pvc
  namespace: elk
spec:
  storageClassName: default
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi